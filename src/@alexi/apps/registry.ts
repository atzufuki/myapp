import { ServiceConfig, ManagerConfig, AppConfig } from '@alexi/apps/config';

export const managers: {
  [key: string]: ManagerConfig;
} = {};

export const services: {
  [key: string]: ServiceConfig;
} = {};

export const apps: {
  [key: string]: AppConfig;
} = {};
